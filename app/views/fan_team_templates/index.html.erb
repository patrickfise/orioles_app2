<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All fan teams
    </h1>

    <a href="/fan_teams/new" class="btn btn-block btn-outline-secondary">
      Add a new fan team
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          User
        </th>

        <th>
          Team
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @fan_teams.each do |fan_team| %>
      <tr>
        <!-- Display fan_team.id start -->
        <td>
          <%= fan_team.id %>
        </td>
        <!-- Display fan_team.id end -->

        <!-- Display fan_team.user_id start -->
        <td>
          <% if fan_team.user.present? %>
            <a href="/users/<%= fan_team.user.id %>">
              <%= fan_team.user.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display fan_team.user_id end -->

        <!-- Display fan_team.team_id start -->
        <td>
          <% if fan_team.team.present? %>
            <a href="/teams/<%= fan_team.team.id %>">
              <%= fan_team.team.created_at %>
            </a>
          <% end %>
        </td>
        <!-- Display fan_team.team_id end -->

        <!-- Display fan_team.created_at start -->
        <td>
          <%= time_ago_in_words(fan_team.created_at) %> ago
        </td>
        <!-- Display fan_team.created_at end -->

        <!-- Display fan_team.updated_at start -->
        <td>
          <%= time_ago_in_words(fan_team.updated_at) %> ago
        </td>
        <!-- Display fan_team.updated_at end -->

        <td>
          <a href="/fan_teams/<%= fan_team.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @fan_teams, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
