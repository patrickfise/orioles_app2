<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All opponent teams
    </h1>

    <a href="/opponent_teams/new" class="btn btn-block btn-outline-secondary">
      Add a new opponent team
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @opponent_teams.each do |opponent_team| %>
      <tr>
        <!-- Display opponent_team.id start -->
        <td>
          <%= opponent_team.id %>
        </td>
        <!-- Display opponent_team.id end -->

        <!-- Display opponent_team.created_at start -->
        <td>
          <%= time_ago_in_words(opponent_team.created_at) %> ago
        </td>
        <!-- Display opponent_team.created_at end -->

        <!-- Display opponent_team.updated_at start -->
        <td>
          <%= time_ago_in_words(opponent_team.updated_at) %> ago
        </td>
        <!-- Display opponent_team.updated_at end -->

        <td>
          <a href="/opponent_teams/<%= opponent_team.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
